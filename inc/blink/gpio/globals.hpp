#pragma once

#include <blink/memory_map.hpp>

#include "gpio.hpp"

namespace gpio
{
    extern gpio& gpio_a;
    extern gpio& gpio_b;
    extern gpio& gpio_c;
    extern gpio& gpio_d;
    extern gpio& gpio_e;
    extern gpio& gpio_h;
} // namespace gpio