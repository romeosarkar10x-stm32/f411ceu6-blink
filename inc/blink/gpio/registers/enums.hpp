#pragma once

#include <blink/types.hpp>

namespace gpio
{
    namespace registers
    {
        namespace enum_bit
        {
            enum type : size_t
            {
                OFF,
                ON,
            };
        }
    } // namespace registers
} // namespace gpio